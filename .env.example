# Transport mode: "stdio" for Claude Desktop, "http" for web integrations
MCP_TRANSPORT=stdio

# HTTP mode configuration (only used when MCP_TRANSPORT=http)
MCP_PORT=3000

# Redirect URL for GET requests (browser access)
REDIRECT_URL=https://your-company.com

# Optional: Default API key for stdio mode
# For HTTP mode, API keys are provided via X-API-KEY header per request
# EUROPARCEL_API_KEY=your-api-key-here 